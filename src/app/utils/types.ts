export type Content = {
    id: string
    title: string,
    description: string,
    source: string,
    media_url: string,
    visitor_count: number
}

export type Data = {
        data : Content[]
}